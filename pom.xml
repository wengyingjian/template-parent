<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>1.3.0.RELEASE</version>
	</parent>
	
	<groupId>com.asd.template</groupId>
	<artifactId>template-parent</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>


	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>
		<java.version>1.8</java.version>
		<mybatis.version>3.2.8</mybatis.version>
		<mybatis-spring.version>1.2.3</mybatis-spring.version>
		<tomcat-jdbc.version>8.0.23</tomcat-jdbc.version>
		<hessian.version>4.0.37</hessian.version>
		<commons-lang3.version>3.1</commons-lang3.version>
		<template.version>2.0-SNAPSHOT</template.version>
		<template.log4j2.version>${template.version}</template.log4j2.version>
		<template.redis.version>1.2-SNAPSHOT</template.redis.version>
		<template.rpc.version>${template.version}</template.rpc.version>
		<template.utils.version>1.0-SNAPSHOT</template.utils.version>
		<template.datasource.version>${template.version}</template.datasource.version>
		<template.rpc.client.version>${template.rpc.version}</template.rpc.client.version>
		<template.rpc.monitor.statsd.version>${template.rpc.version}</template.rpc.monitor.statsd.version>
		<rpc.utils.server.version>1.0.1-SNAPSHOT</rpc.utils.server.version>
		<curator.version>2.9.0</curator.version>
	</properties>


	<dependencyManagement>
		
	</dependencyManagement>
</project>